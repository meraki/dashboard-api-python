---
phase: 02-unified-parameter-parser
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/generator/test_parser_v3.py
  - tests/generator/fixtures/parse_params_v3_golden.json
autonomous: true
requirements: [PARSE-03, PARSE-04, PARSE-05, PARSE-06]

must_haves:
  truths:
    - "Golden file captures full parse_params_v3 output for a complex operation"
    - "Snapshot test fails if parser output format changes unexpectedly"
    - "Golden file documents the expected output contract for Phase 3 consumers"
  artifacts:
    - path: "tests/generator/fixtures/parse_params_v3_golden.json"
      provides: "Expected output snapshot for parse_params_v3"
      contains: "organizationId"
    - path: "tests/generator/test_parser_v3.py"
      provides: "Golden file snapshot test"
      contains: "test_golden_file"
  key_links:
    - from: "tests/generator/test_parser_v3.py::test_golden_file"
      to: "tests/generator/fixtures/parse_params_v3_golden.json"
      via: "json.load comparison"
      pattern: "parse_params_v3_golden"
---

<objective>
Create golden-file snapshot test that locks the parse_params_v3 output format. Exercises the most complex fixture path (getOrganizationDevices: path-level param inheritance, oneOf, array, nullable, pagination) and compares output against a committed JSON file.

Purpose: Prevents accidental output format changes. Serves as documentation of the exact output contract for Phase 3 (generation integration).
Output: Golden JSON file + snapshot test method.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unified-parameter-parser/02-CONTEXT.md
@.planning/phases/02-unified-parameter-parser/02-01-SUMMARY.md

<interfaces>
<!-- parse_params_v3 contract from Plan 01 -->
From generator/parser_v3.py:
```python
def parse_params_v3(operation: dict, path_item: dict, spec: dict) -> tuple[dict, dict]:
    """Returns (params_dict, metadata_dict) where metadata has content_type."""
```

Expected output for getOrganizationDevices (path /organizations/{organizationId}/devices GET):
- organizationId: from path-level $ref, in=path, required=True
- networkId: in=query, nullable=True
- startDate: in=query, type="object or string", description includes "(object supports: gt, lt)"
- tags: in=query, type=array, style=form, explode=True, items={type:string}
- perPage: in=query, type=integer
- total_pages: injected by generate_pagination_parameters
- direction: injected by generate_pagination_parameters
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate golden file and add snapshot test</name>
  <files>tests/generator/fixtures/parse_params_v3_golden.json, tests/generator/test_parser_v3.py</files>
  <read_first>
    - tests/generator/test_parser_v3.py
    - tests/generator/fixtures/synthetic_v3_spec.json
    - generator/parser_v3.py
  </read_first>
  <action>
1. Write a one-off script (or inline in test) to generate the golden file. Run parse_params_v3 against the getOrganizationDevices operation from the fixture, serialize the (params_dict, metadata_dict) tuple as JSON, and save to `tests/generator/fixtures/parse_params_v3_golden.json`.

The golden file format:
```json
{
  "params": {
    "organizationId": {"required": true, "in": "path", "type": "string", "description": "Organization ID", "nullable": false},
    "networkId": {"required": false, "in": "query", "type": "string", "description": "Filter by network", "nullable": true},
    "startDate": {"required": false, "in": "query", "type": "object or string", "description": "Start date filter (object supports: gt, lt)", "nullable": false},
    "tags": {"required": false, "in": "query", "type": "array", "description": "Filter by tags", "nullable": false, "items": {"type": "string"}, "style": "form", "explode": true},
    "perPage": {"required": false, "in": "query", "type": "integer", "description": "Items per page", "nullable": false},
    "total_pages": {"type": "integer or string", "description": "use with perPage to get total results up to total_pages*perPage; -1 or \"all\" for all pages"},
    "direction": {"type": "string", "description": "direction to paginate, either \"next\" (default) or \"prev\" page"}
  },
  "metadata": {
    "content_type": null
  }
}
```

NOTE: The exact values above are the EXPECTED output. Generate the golden file by actually running parse_params_v3 and dumping JSON. If the output differs from expected, fix the golden file to match actual parser output (parser behavior is authoritative after Plan 01 tests pass).

2. Add `test_golden_file` method to `TestParseParamsV3` class in test_parser_v3.py:

```python
def test_golden_file(self, v3_spec):
    """Snapshot test: parse_params_v3 output matches committed golden file."""
    path = "/organizations/{organizationId}/devices"
    operation = v3_spec["paths"][path]["get"]
    path_item = v3_spec["paths"][path]

    params, metadata = parse_params_v3(operation, path_item, v3_spec)

    golden_path = FIXTURES / "parse_params_v3_golden.json"
    with open(golden_path) as f:
        expected = json.load(f)

    assert params == expected["params"]
    assert metadata == expected["metadata"]
```

3. Run: `python -m pytest tests/generator/test_parser_v3.py::TestParseParamsV3::test_golden_file -v`

4. Run full suite: `python -m pytest tests/generator/ -v`

Commit: `test(02-02): add golden-file snapshot test for parse_params_v3`
  </action>
  <verify>
    <automated>cd C:/Users/jkuchta/Work/_Repos/meraki/dashboard-api-python && python -m pytest tests/generator/test_parser_v3.py::TestParseParamsV3::test_golden_file -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/generator/fixtures/parse_params_v3_golden.json exists and is valid JSON
    - tests/generator/fixtures/parse_params_v3_golden.json contains "organizationId"
    - tests/generator/fixtures/parse_params_v3_golden.json contains "object or string"
    - tests/generator/fixtures/parse_params_v3_golden.json contains "content_type"
    - tests/generator/test_parser_v3.py contains "def test_golden_file"
    - tests/generator/test_parser_v3.py contains "parse_params_v3_golden.json"
    - pytest tests/generator/test_parser_v3.py::TestParseParamsV3::test_golden_file exits 0
    - pytest tests/generator/ exits 0 (full suite, no regressions)
  </acceptance_criteria>
  <done>Golden file committed. Snapshot test passes. Any future parser output change will break this test, protecting the contract for Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Golden file test is pure validation, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Golden file manual editing | accept | Test re-generates from parser; manual edits without matching parser change will fail |
</threat_model>

<verification>
```bash
# Golden file test passes
python -m pytest tests/generator/test_parser_v3.py::TestParseParamsV3::test_golden_file -v

# Full suite passes
python -m pytest tests/generator/ -v

# Golden file exists with expected content
python -c "import json; d=json.load(open('tests/generator/fixtures/parse_params_v3_golden.json')); print(len(d['params']), 'params')"
```
</verification>

<success_criteria>
- Golden file exists at tests/generator/fixtures/parse_params_v3_golden.json
- Snapshot test compares parse_params_v3 output against golden file
- Test passes (output matches)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-unified-parameter-parser/02-02-SUMMARY.md`
</output>
