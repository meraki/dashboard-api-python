name: Test Python library

on:
  push:
    branches: ['main', 'release', 'dev_rest_session']
    paths-ignore:
      - '.github/**'
      - 'examples/**'
      - 'meraki/aio/**'
      - 'notebooks/**'
      - 'generator/**'
      - 'README.md'
      - 'LICENSE'
  pull_request:
    branches: ['main', 'release', 'dev_rest_session']
    paths-ignore:
      - '.github/**'
      - 'examples/**'
      - 'meraki/aio/**'
      - 'notebooks/**'
      - 'generator/**'
      - 'README.md'
      - 'LICENSE'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true

    - name: Set up Python ${{ matrix.python-version }}
      run: uv python install ${{ matrix.python-version }}

    - name: Install dependencies
      run: uv sync --python ${{ matrix.python-version }}

    - name: Lint with flake8
      run: |
        uv run flake8 . --count --select=E9,F63,F7,F82 --ignore=F405,W391,W291,C901,E501,E303,W293 --exclude=examples,generator,.venv --show-source --statistics
        uv run flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --exclude=examples,generator,.venv --statistics

    - name: Test with pytest
      run: uv run pytest --apikey ${{ secrets.TEST_ORG_API_KEY }} --o ${{ secrets.TEST_ORG_ID }}
