`DashboardAPI` and `AsyncDashboardAPI` now expose the `nac`, `secureConnect`, `support`, `users`, and `assistant` categories. They were generated but never registered, so accessing them raised `AttributeError` even though the underlying API methods worked.
